export default {
    server: [
        window.location.origin + window.location.pathname,
        'https://raw.githubusercontent.com/Dituon/petpet/main'
    ],
    alternateFont: [
        'MiSans-Bold',
        'sans-serif'
    ].join(',')
}
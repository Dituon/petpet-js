export default {
    server: [
        window.location.origin + window.location.pathname,
        'https://d2n.moe/petpet'
    ],
    alternateFont: [
        'MiSans-Bold',
        'sans-serif'
    ].join(',')
}